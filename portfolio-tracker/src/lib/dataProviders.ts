/**
 * Data-provider boundary for future work (Finnhub, Polygon, Alpha Vantage).
 * All live screener data currently flows through `@/lib/fmp/client` and
 * `@/lib/screener/fmpScreenerData`.
 */

export type SupportedQuoteProvider = "fmp";
